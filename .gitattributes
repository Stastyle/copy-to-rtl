# Normalize line endings: store text as LF in the repo.
* text=auto eol=lf

# Windows scripts must stay CRLF on checkout.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# Treat these as binary (no diff / no EOL normalization).
*.png binary
*.ico binary
